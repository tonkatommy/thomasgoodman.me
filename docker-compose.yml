# =============================================================================
# Docker Compose Configuration
# =============================================================================
# Development and production configurations for the portfolio website
#
# USAGE:
# Development: docker-compose up
# Production:  docker-compose -f docker-compose.yml -f docker-compose.prod.yml up
# =============================================================================

services:
    # ---------------------------------------------------------------------------
    # Next.js Application
    # ---------------------------------------------------------------------------
    app:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: thomasgoodman-app
        ports:
            - "3000:3000"
        environment:
            - NODE_ENV=development
            - NEXT_PUBLIC_SITE_URL=http://localhost:3000
        volumes:
            # Mount source code for hot reloading in development
            - .:/app
            - /app/node_modules
            - /app/.next
        restart: unless-stopped

    # ---------------------------------------------------------------------------
    # PostgreSQL Database (Optional - for blog/CMS features)
    # ---------------------------------------------------------------------------
    # Uncomment when database is needed
    # db:
    #     image: postgres:16-alpine
    #     container_name: thomasgoodman-db
    #     environment:
    #         POSTGRES_USER: portfolio
    #         POSTGRES_PASSWORD: ${DB_PASSWORD:-localdev}
    #         POSTGRES_DB: portfolio
    #     ports:
    #         - "5432:5432"
    #     volumes:
    #         - postgres_data:/var/lib/postgresql/data
    #     restart: unless-stopped

# -----------------------------------------------------------------------------
# Volumes
# -----------------------------------------------------------------------------
# volumes:
#     postgres_data:
